#!/bin/bash
echo "========================================================"
echo "Starting Mini Vault using Docker Compose..."
echo "This will build the containers and start the app."
echo "========================================================"
docker compose up --build
